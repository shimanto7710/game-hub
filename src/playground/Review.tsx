import React from "react";
import { Typography } from "@mui/material";

const Review: React.FC = () => {
  return <Typography variant="h4">Welcome to Review Page</Typography>;
};

export default Review;
